# คัดลอกเป็น terraform.tfvars แล้วใส่ค่าจริง — ห้าม commit ไฟล์ terraform.tfvars
#
# 💡 แนะนำ: อย่าใส่ hcloud_token ในไฟล์นี้ (token จะลงดิสก์) — ส่งทาง env แทน
#    read -rs TF_VAR_hcloud_token && export TF_VAR_hcloud_token
# hcloud_token = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

ssh_public_key = "ssh-ed25519 AAAA... you@example.com"
mail_hostname  = "mail.perpos.ai"
server_type    = "cx23"
location       = "nbg1" # เยอรมนี — ห้ามใช้ datacenter (Hetzner เลิกใช้แล้ว)
                        # ⚠️ cx23 มีเฉพาะยุโรป · ถ้าเปลี่ยน location ต้องเปลี่ยน server_type ด้วย

# IP ที่ให้ SSH เข้าได้ — ระบุให้แคบที่สุดเท่าที่ทำได้
ssh_allowed_ips = ["0.0.0.0/0", "::/0"]
